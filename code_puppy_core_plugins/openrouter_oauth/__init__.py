"""OpenRouter OAuth PKCE plugin.

One-click browser sign-in that exchanges an authorization code for a
user-controlled OpenRouter API key, wired into the ``/add_model`` flow via
the ``provider_credential_flow`` hook.
"""
